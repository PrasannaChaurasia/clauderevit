node_modules/
mcp-server/node_modules/
*.env
.env*

# API key lives here — never commit the real config
extension/ClaudeRevit.extension/config.json

# MCP config contains API keys — use config.example.json as reference
.mcp.json
